#include <vector>
#include <string.h>
#include <iostream>

using namespace std;

string left;
string right;
int n;
int k;

bool canHe(int idx = 0, int water_level, bool is_left = true)
{
    if (idx >= n - 1)
        return true;
}

int main()
{
    cin >> n >> k;
    cin >> left;
    cin >> right;
}